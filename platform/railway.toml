[build]
builder = "nixpacks"
buildCommand = "./gradlew bootJar -x test"

[deploy]
startCommand = "java -Dspring.profiles.active=prod -Dfile.encoding=UTF-8 -Xmx400m -Xms200m -jar build/libs/platform-0.0.1-SNAPSHOT.jar"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
